from setuptools import setup, find_packages

setup(
    name='structparser',
    version='1.1',
    author='Luiz Baraldo',
    url='https://github.com/Baraldo/struct-parser',
    packages=find_packages()
)